import express from "express";
const router = express.Router();

router.post("/courses/:courseId", );
router.get("/courses/:courseId", );
router.get("/courses/:courseId/sessions/:sessionId", );


export default router;
